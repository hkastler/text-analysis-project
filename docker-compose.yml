version: '3'

networks:
  docker-microservice-tool-stack_webnet:
    external: true

services:
  tomee:
    build: .
    ports:
      - "8080:8080"
      - "8443:8443"
    volumes:
     - C:\etc\opt:/etc/opt
    networks:
    - docker-microservice-tool-stack_webnet
    hostname: "text-analysis-project"
